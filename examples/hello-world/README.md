# Hello world

Minimal React host for `@aibar/react`.

```bash
cd packages && npm install && npm run build
cd examples/hello-world && npm run dev
```

Five steps: install, mount, register items, feed context (optional), persist
(optional). See [host guide](../../docs/aibar-host-guide.md).
