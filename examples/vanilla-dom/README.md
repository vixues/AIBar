# Vanilla DOM

Same demo as hello-world, without React: `@aibar/core` + `@aibar/renderer-dom`.

```bash
cd packages && npm install && npm run build
cd examples/vanilla-dom && npm run dev
```

Open the printed localhost URL. Click **Send**, switch Flash/Pro, drag
temperature, then shrink the window until items overflow.
