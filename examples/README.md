# Examples

One playground: React at `/`, vanilla DOM at `/vanilla.html`. Catalog, theme,
and page chrome are shared.

```bash
cd .. && npm run build
cd examples/playground && npm run dev
```

Screenshots in the [root README](../README.md). Refresh with `npm run capture:readme` from `packages/`.
