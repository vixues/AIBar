# @aibar/intent

Optional `HeuristicIntentEngine` for the suggestion lane. Stores action
identities and context keys — never user content. Never emits `destructive`
suggestions (INV-A8).

Wire via `adapter.intent.predict`.
