/**
 * @aibar/renderer-dom — DOM RendererBackend (docs/aibar-architecture.md §7.4).
 * Import './styles.css' once per app (the React binding does this).
 */
export { DOMRendererBackend, type DOMRendererOptions } from './renderer';
